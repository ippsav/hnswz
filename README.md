# hnswz

A vector database written in Zig.

Feed it text, it embeds locally via [Ollama](https://ollama.com), stores the vectors, and finds nearest neighbors fast using an HNSW index.

> **Status:** Work in progress
